def count_bits(n):
    count = 0      # initial count will be 0
    while(n>0):    
        if n%2:    # checks the bit 1 or 0
            count +=1
        n //=2     # reduce n by 2 
    return count
